describe('Bateria de tests', () => {
  test('Test 1', () => {
    const a = true;

    expect(a).toBe(true);
  });

  test('Test 2', () => {
    const b = 'keepcoding';

    expect(b).toBe('keepcoding');
  });
});
